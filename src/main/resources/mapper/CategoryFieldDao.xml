<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pro.dengyi.myhome.dao.CategoryFieldDao">
    <select id="selectListByDeviceId" resultType="pro.dengyi.myhome.model.CategoryField">
        SELECT category_field.id,
               category_field.field,
               category_field.field_type,
               category_field.category_id
        FROM category_field
                 INNER JOIN device_category ON device_category.id = category_field.category_id
                 INNER JOIN device ON device.category_id = device_category.id
        WHERE device.id = #{deviceId}
    </select>
</mapper>